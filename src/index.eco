<!DOCTYPE html>
<html>
<head>
  <title>Open Source at Demandforce</title>
  <link rel="stylesheet" type="text/css" href="stylesheets/screen.css">
</head>
<body>
  <div id="header">
    <h1>Open Source at <a href="#">Demandforce</a></h1>
  </div>
  <div id="content">
    <div id="repositories">
      <div class="wrapper">
        <h1>Some of the cool projects we're working on</h1>
        <% for repo in @repos: %>
          <div class="repository<% unless repo.homepage: %> no-homepage<% end %>">
            <% if repo.homepage: %>
            <a href="<%= repo.homepage %>">
              <img src="images/<%= repo.id %>.png">
            </a>
            <% end %>
            <div class="content">
              <h2><a href="<%= repo.homepage || repo.html_url %>"><%= repo.name %></a></h2>
              <p><%= repo.description %><p>
            </div>
            <div class="meta">
              <span class="view"><a href="<%= repo.html_url %>">View on Github</a></span>
              <span class="watchers"><%= repo.watchers.format(0) %> Watchers</span>
              <span class="forks"><%= repo.forks.format(0) %> Forks</span>
            </div> 
          </div>
        <% end %>
        </div>
      </div>
      <div id="members">
        <div class="wrapper">
          <h1>The people who make this all possible</h1>
          <% for contributor in @contributors: %>
            <div class="member">
              <a href="<%= contributor.html_url %>" title="<%= contributor.name %>">
                <div class="avatar"><img src="<%= contributor.avatar_url %>"></div>
              </a>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
  <div id="footer">
    Generated by <a href="https://github.com/demandforce/oss-pride">OSS Pride</a>
  </div>
</body>
</html>
